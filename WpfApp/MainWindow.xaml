<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d" FontSize="20" Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button x:Name="previousImageButton" Padding="15" Margin="15" Click="previousImageButton_Click">Previous image</Button>
            <Button x:Name="sunInformationButton" Padding="15" Margin="15" Click="SunInformationButton_Click">Sun info</Button>
            <Button x:Name="nextImageButton" Padding="15" Margin="15" Click="nextImageButton_Click">Next image</Button>
        </StackPanel>
        <Image Grid.Row="1" x:Name="comicImage" Margin="0,0,0,-44"></Image>
    </Grid>
    
</Window>
